# ---- run OUTSIDE the DAFoam container (Phases 1-2, postprocessing) ----
numpy>=1.24
matplotlib>=3.7
classy-blocks>=1.5        # Phase 1: scripted blockMeshDict generation
pyvista>=0.43             # postprocessing: read OpenFOAM results

# ---- run INSIDE the DAFoam environment (Phases 3-5) ----
# DAFoam itself is NOT pip-installable: use the Docker image
#   docker pull dafoam/opt-packages:latest
# which ships OpenFOAM v1812(+HiSA), DAFoam, pyGeo, IDWarp, pyHyp,
# OpenMDAO, MPhys, mpi4py, petsc4py. Then ADD GEMSEO inside it:
gemseo>=5.3,<6            # Phase 5 optimizer framework (v6 renamed MDODiscipline)
